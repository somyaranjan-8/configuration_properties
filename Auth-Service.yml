spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/
  datasource:
    username: root
    password: somya
    url: jdbc:mysql://localhost:3306/somya

management:
  endpoints:
    web:
      exposure:
        include: '*'
